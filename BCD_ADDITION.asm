; 
.DATA
DATA1 DB 0DFH
DATA2 DB 66H
RES DB ?


.CODE
START:
    MOV AX, DATA
    MOV DS, AX
    XOR AX, AX
    XOR BX, BX
    MOV AL, DATA1
    ADD AL, DATA2
    DAA ;ADJUST BCD ADDITION
    MOV BL, AL ;RESULT STORED TO THE BL REGISTER
    MOV RES, BL
    
    INT 21H




END START