; BCD TO 8 BIT ASCII NUMBER

.DATA

BCD_DATA DB 8H
ASCII_RESULT DB ? ;38

.CODE
START:
    MOV AX, DATA
    MOV DS, AX
    XOR AX, AX 
    MOV AL, BCD_DATA
    OR AL, 30H
    MOV ASCII_RESULT, AL
    XOR DX, DX
    MOV DL, AL
    INT 21H
    



END START